import { beforeAll, afterAll } from 'vitest';

beforeAll(async () => {
  // E2E test setup — start app, seed database
  // Will be implemented when full app is ready
});

afterAll(async () => {
  // Cleanup
});
